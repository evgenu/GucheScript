# this is a comment!

func main {
    int a = 5^2^3 + 10

    # arithmetic logic
    int b = a + 5 * (5 - 10) * 5

    # without a value
    char d 

    if ( a > 5 ) {
        a = 5
        int c = 10
    }

    while (c < 15) {
        c = c + 1
    }
}